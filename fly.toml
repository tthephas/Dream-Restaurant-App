# fly.toml file generated for dreamrestaurant on 2023-02-08T09:22:03-06:00

app = "dreamrestaurant"
kill_signal = "SIGINT"
kill_timeout = 5
processes = []

[build]
  builder = "heroku/buildpacks:20"

[env]
  PORT = "8080"
  SECRET = "imgonnabuildanewrestaurantdbthisweek"
  DATABASE_URL = "mongodb+srv://thepjos:4ker2ns0@cluster0.x5cdogj.mongodb.net/Dream_Restaurant?retryWrites=true&w=majority"
  MONGO_DEV_URI = "mongodb://localhost/Dream_Restaurant"
  CLIENT_ORIGIN = "this will be the front end's deployed application"

[experimental]
  auto_rollback = true

[[services]]
  http_checks = []
  internal_port = 8080
  processes = ["app"]
  protocol = "tcp"
  script_checks = []
  [services.concurrency]
    hard_limit = 25
    soft_limit = 20
    type = "connections"

  [[services.ports]]
    force_https = true
    handlers = ["http"]
    port = 80

  [[services.ports]]
    handlers = ["tls", "http"]
    port = 443

  [[services.tcp_checks]]
    grace_period = "1s"
    interval = "15s"
    restart_limit = 0
    timeout = "2s"
